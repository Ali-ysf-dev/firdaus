/**
 * Connector overlays (SVG lines from the 3D model to copy) were removed for a cleaner layout.
 * Props are kept so callers stay unchanged.
 */
export default function StoryCallouts() {
  return null;
}
